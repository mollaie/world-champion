import { SeasonFilterPipe } from './season-filter.pipe';

describe('SeasonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
